// Objetos matemáticos
/*
    // Propriedades matemáticas

        console.log(Math.PI) // PI.
        console.log(Math.E) // Número de Euler.
        console.log(Math.SQRT2) // Raiz quadrada de 2.
        console.log(Math.SQRT1_2) // Raiz quadrada de 1/2.
        console.log(Math.LN2) // Logarítmo natural de 2
        console.log(Math.LN10) // Logarítmo natural de 10
        console.log(Math.LOG2E) // Logarítmo do número de Euler na base 2.
        console.log(Math.LOG10E) // Logarítmo do número de Euler na base 10.

    // Métodos matemáticos

        // Tranformando Number em Integer

            var pi = Math.PI + 0.8

            console.log(pi)
            console.log(Math.round(pi))
            console.log(Math.ceil(pi))
            console.log(Math.floor(pi))
            console.log(Math.trunc(pi))
*/
        // Outros métodos

            var pi = Math.PI

            console.log(Math.sign(-pi)) // Sinal do valor
            console.log(Math.pow(pi, Math.E)) // Exponenciação
            console.log(Math.sqrt(pi)) // Raiz quadrada
            console.log(Math.abs(-pi)) // Valor absoluto (sem sinal)
            console.log(Math.sin((90 * pi) / 180)) // Seno
            console.log(Math.cos((180 * pi) / 180)) // Cosseno
            console.log(Math.random()) // Número aleatório entre 0 e 1.
            console.log(Math.log(10)) // Logarítmo
            console.log(Math.log2(10)) // Logarítmo na base 2
            console.log(Math.log10(10)) // Logarítmo na base 10
